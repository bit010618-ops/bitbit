# Findings

## 2026-07-14 最新修复验证
- 数值“1”与样值竖线重合的根因是 `value_label_offsets` 横向偏移不足；由 9 pt 调整为 13 pt 后，标签仍贴近样值点且不再压线。
- 中文字体不包含 Unicode 下标 `₁/₂`，直接替换会渲染为空方框；最终改用透明 LaTeX 数学图片嵌入红色中文说明行，渲染结果正常。
- 第十二批前四页重排后的内容顺序、标题、公式和图形均在页面范围内，无新增打架或裁切。

## 2026-07-14 第十二批审计启动
- 当前 Git 工作树干净，最新版提交为 `b99492b`，完整成品 PDF 为 97 页。
- 第十二批脚本 `work/make_dsp_batch_367_399_redraw.py` 已确认含有多处概括性绘图函数，如 `ideal_filters`、`window_flow`、`window_effect`、`zero_plane`、`sampled_response`；这些图只能视为待核对对象，不能直接认定与原课件 367-399 页一致。
- 新增持续交付规则：每次生成或更新完整成品 PDF 后，必须在验证通过后提交并推送 GitHub，只保留最新版完整成品。
- 视觉对照确认原课件 367-399 共 33 页，而当前第十二批仅 5 页。当前批次不是单纯排版差异，而是摘要化重写，无法满足“不删减、逐图一致”。
- 明确缺失或过度压缩的内容包括：原 367-368 两个线性相位判断例题的完整推导；369-372 零点性质的变换推导和结论；375-378 四类理想滤波器逐页公式、图形及红字说明；379-381 窗函数法流程、截断前后时频域六图及 Gibbs 现象原图；382 五种窗函数完整定义；383 指标表和经验公式；384-389 窗函数设计步骤与例题逐步计算；390-397 频率采样法基本思想、约束条件、设计步骤、例题各步骤；398 章节导图；399 完整课后题说明。
- 当前 5 页中的 `ideal_filters`、`window_flow`、`window_effect`、`zero_plane`、`sampled_response` 仅是概括图，需按源页独立模板重画或拆页，不能视为已通过。
- 第十二批重建后由 5 页扩展为 17 页；三张联系表显示无页面截断、无元素打架，源 367-399 的六个内容组已按独立页面展开。
- 视觉复核发现第 9 页经验公式中的中文 `窗函数类型` 被 mathtext 替换为空方块。根因是中文被放入数学公式；应将中文说明移到公式外，数学块只保留符号关系。

## 2026-07-13 第一章逐图复核（实时证据）
- 原第一章 PDF 第 3 页包含两幅独立图：上方连续模拟曲线；下方离散时域样点图（横轴 -2..2，纵轴刻度 1、2，样点约为 0、0.8、0.5、1.5、0）。当前讲义第 2 页只保留分类表，缺失这两幅图，状态：`mismatch / missing`。
- 原第一章 PDF 第 4 页流程框图为黑色直角矩形和黑色直线箭头，完整标签依次为 `x(t)`、`预滤波`、`A/D转换`、`数字信号处理`、`D/A转换`、`平滑滤波`、`y(t)`。当前讲义第 2 页使用蓝色圆角框和缩写 `A/D`、`DSP`、`D/A`，状态：`mismatch`。
- 重新生成第一批后，讲义批次第 1 页已恢复原第 3 页的连续曲线和离散样点图；离散样值改为 `{-2:0,-1:0.8,0:0.5,1:1.5,2:0}`，纵轴 `1/2` 两个短刻度已恢复，`n=0` 样点位于 0 与 1 之间。该图状态：`verified on rendered batch page 1`。
- 重新生成第一批后，处理链已改为黑色直角框、黑色直线箭头和完整中文标签，状态：`verified on rendered batch page 1`。
- 原第 7 页 `R_N(n), N=2` 图与批次第 3 页当前几何和样值一致；原点标签按用户后续统一规范稍向纵轴右侧放置。状态：`verified`。
- 原第 12 页九幅余弦序列图在讲义中按分页保留：批次第 3 页开始第一行三图，后续页继续其余六图；必须再核对后续页是否完整九图及每个频率标签。
- 原第 14 页尺度变换基准序列原先整体右移 1 个单位，已修正为 `{-3:3,-2:2,-1:4,0:2,1:2,2:3,3:1}`；`x(n/2)` 与 `x(2n)` 均由修正后的序列生成。三图横轴只保留原图的 `-1/0/1` 刻度标签，不再显示通用端点标签。渲染状态：`verified on batch page 4`。
- 原第 16 页例 2 坐标图已改为专用轴限：仅标 `-1/0/1`，纵轴下半段延伸至 -2 样点以下，图名 `x(1-2n)` 放回纵轴上方。渲染状态：`verified on batch page 5`。
- 原第 25 页卷积图解的两张小坐标图已去除通用绘图器擅自添加的横轴刻度和逐点数值；左图从纵轴向右衰减，右图样点从最右纵轴向左展开，且只在纵轴旁保留一个幅值 `1`。渲染状态：`verified on batch 2 page 2`。
- 原第 87 页 DTFT 共轭分量三图原先被改成黑色横向三栏；已恢复蓝色坐标轴、红色样点/幅值、`h_e(n) → h_o(n) → h(n)` 纵向排列，并补回 `h_o(0)=0` 原点样点。渲染状态：`verified on batch 3 page 8`。

## User-reported regressions
- 完整讲义第 28 页出现空白页。
- 早期批次未同步后续公式、图形、红色重点和“不删减”要求。
- 第 51 页及所有 FFT 蝶形流图与原课件差异大。
- 第 55-60 页的系统框图、IIR 结构图和频率响应图偏离原图，存在箭头、文字与边框重叠。
- 第 64、67-71 页存在重叠、FIR 结构图偏离原图、遗漏原课件红色重点。
- 已知图形问题包括坐标轴箭头顶住原点/样点、反馈线穿过方块、公式与图形相撞、标签不在正确位置。

## Validation standard
- 每次修改后，使用 PDF 渲染图逐页检查：无空白页、无重叠、页眉/页脚正确、图形连线和红色重点与原课件一致。
- 每一张图必须记录原课件页与讲义页映射，并逐项核对：拓扑结构、节点数、线序、箭头方向、文字标签、系数位置、坐标刻度、颜色重点和相对比例。
- 最终只输出一份彩印友好版，不再派生黑白版；非必要装饰色移除，但原课件的必要颜色必须保留。

## 彩印友好版式参考
- 用户指定参考文件：`C:/Users/HP/Desktop/讲义、做题本/【A4】小马哥960题目册做题本.pdf`，A4 纵向，共 416 页。
- 参考文件采用白底、黑色正文、页眉左右小字加细横线、页脚居中页码、无大色块和无装饰卡片；目录使用黑色文字与点引导线。
- 最终 DSP 讲义沿用这种克制页面骨架，但不把原 DSP 课件中本来彩色的公式、重点文字、曲线或框图改黑；颜色仅在原内容需要时出现。
- W3C 和 Adobe 的公开设计建议均强调颜色不能作为唯一信息通道，并应使用小型角色化调色板、优先保证对比度；本讲义以标签、线型、位置和文字同时表达含义，颜色只作原内容强调。

## Root-cause mapping
- 最终第 28 页来自第 4 批（原 PPT 89-114 页）的第 7 页，正文仅 31 字符，属真实空白页。
- 最终第 50-53 页来自第 8 批 FFT；最终第 54-61 页来自第 9 批 IIR；最终第 67-70 页来自第 11 批 FIR。
- 原课件对照已渲染完成：FFT 第 18 页为 DIT 三层蝶形分区图，第 23-24 页为 DIF 运算流图；IIR 第 10、12 页为直接 I/II 型参考；FIR 第 5、10 页为直接型与频率采样结构参考。
- FFT 仍需处理奇偶分解页和 DIT 第三级的连线可读性；IIR 的并联结构和四类理想滤波器页仍保留了早期“概括图”痕迹，须以原课件节点关系重绘。
- 完整稿加入目录后，原用户指出的页号整体后移一页；后续验收以“原章节内容 + 原课件页”对应，而不只依赖旧页码。
- 第十批末尾曾产生一个正文长度为 73 字符但视觉近空白的页；根因是“章节导图与课后题”被分页拆分，已通过压缩同页表格和总结文本解决，未删除习题或公式。
- 早期第二批原例 9/10 的斜向反馈线是打架根因；已改为正交反馈路径。原坐标轴组件的留白不足亦已在通用组件层修复。

## 第 1-2 章逐图对照（执行中）
- 原课件第 3 页的“信号分类”包含模拟信号曲线图和时域离散信号样点图；当前讲义第 2 页用表格替代，图形被遗漏，不符合逐图一致标准。
- 原课件第 4 页流程图为直角黑框、黑色直线箭头，标签依次为 `x(t) - 预滤波 - A/D转换 - 数字信号处理 - D/A转换 - 平滑滤波 - y(t)`；当前讲义使用蓝色圆角框、标签 `x_a(t)`、`A/D`、`DSP`、`D/A`、`y_a(t)`，几何与文字均不一致，需按原图独立坐标模板重绘。
- 原课件第 14 页包含 `x(n)`、`x(n/2)`、`x(2n)` 三幅并列序列变换图；当前讲义对应段落未完整保留，属于明确漏图。
- 原课件第 7 页的矩形序列定义旁有 `N=2` 的样点图，当前讲义第 3 页只保留分段公式表，样点图遗漏。
- 原课件第 12 页有 9 幅不同频率的余弦序列图，当前讲义第 4 页仅保留 4 幅概括图；必须恢复完整九图组，并按原三行三列位置和原标签复刻。
- 原课件第 6 页的集合表示示例图当前讲义虽保留，但当前图按集合数值重画；原图自身可能存在样值与文字不一致，需要按“修正错误但保持原轴、标签和相对位置”的规则处理。
## 第五批频响/全通公式修正（2026-07-13）
- `f_s/2` 与 `f_s` 不能用普通字符串或过小公式图；应使用 LaTeX 渲染，保持与 `pi`、`2pi` 的中心线一致，并给右侧单位留出间距。
- 长分式与条件说明放在同一行会触发等比压缩。全通一般式应拆成“主体公式”与“条件”两行，主体优先保证打印可读字号。
- 用户明确要求该全通一般式只放大，不因放大而额外标红或标重点；主公式与条件均保持普通黑色，原课件说明句完整保留。
## 第六、七批坐标图修正（2026-07-13）
- 时域/频域关系图不能用“相似的随机柱状图”代替。四个象限必须由同一信号生成：主值序列、周期延拓、周期连续频谱和等间隔离散谱相互对应。
- 相邻 `N-1`、`N` 标签必须错层，说明框应放在绘图区外，禁止压住谱线。
- 截断效应双谱线图必须使用源图符号 `-omega_0`、`omega_0`、`pi`，不得用内部数值 `-1`、`1`、`3.14` 直接显示。

## 第五章 IIR 结构逐图复核（2026-07-13）
- 原课件第 272 页与当前第九批第 2 页并排复核：二阶滤波器方框图和信号流图的输入、`b_0`、两级 `z^{-1}`、反馈系数 `a_1/a_2`、两个加法器及箭头方向均已对应；当前差异主要是 A4 等比缩放和章节正文接排，不构成拓扑偏差。该页状态：`topology verified, final page-spacing audit pending`。
- 原课件第 274 页为 IIR 基本网络公式与“直接型/级联型/并联型”说明，第 275 页为直接 I 型结构。当前第九批第 3 页的直接 I 型保留输入横向延时链、输出反馈延时链、`b_0...b_M` 与 `a_1...a_N` 的方向和分区，但因与直接 II 型同页而缩小；后续要以打印可读性为准决定是否独立分页放大，不能为了填满页面继续压缩。
# 2026-07-14 全书审校恢复

- 当前唯一成品 `outputs/华理814DSP讲义_完整彩色A4版.pdf` 为 120 页 A4，最近一次完整测试为 165 项通过；这些测试只覆盖已编码规则，不能证明全书逐图与原 PDF 一致。
- `work/figure_audit_matrix.md` 仍将 83 个绘图入口全部标为 `pending`，没有回填后续已经完成的局部源图对照，因此该矩阵目前只能作为待审清单，不能作为完成证据。
- 后续验收必须为每个绘图入口补齐原课件页、讲义页、视觉复验状态和差异记录；未能定位或仅通过源码断言的项目继续视为未完成。
- `tmp/pdfs/full_visual_latest` 仅有第 1-94 页渲染图，早于当前 120 页成品，不能用于全书最终审校；必须从最新 PDF 重新生成 120 页渲染和联系表。
- Codex 内置 Python 当前没有 `fitz`/PyMuPDF，后续渲染需使用已安装的 PDFium、Poppler 或仓库现有渲染工具，不再重复尝试 PyMuPDF。
- 已从最新成品重新渲染 120 页并生成 10 张联系表。联系表第 1-24 页首轮检查未见明显文字/图形重叠或越界；第 7 页的大面积留白位于章节结束页，符合“新章另起一页、章尾可留白”的用户规则。
- 第 1-24 页的联系表只能证明当前版面无明显宏观冲突，尚不能证明图形与原课件逐线一致；对应图仍需结合原课件第 1-88 页逐图对照后才能更新为 verified。
- 最新成品第 25-48 页联系表首检未见页面外溢、图文互压或空白页；第 36 页全通滤波器长分式和第 39 页四联图在联系表缩略图上偏小，需在高分辨率单页中复核打印可读性，不能仅凭联系表判定通过。
- 第 39 页的四联 DFS/DFT 示意、第 47 页流程框图等图形宏观位置正常，但尚未逐线对照原课件，继续保持未验证状态。
- 第 49-72 页联系表首检未见页面越界或明显图文相压。第 55-57 页 FFT 蝶形流图、56-57 页完整流图、61-65 页 IIR 结构图是高风险拓扑页，必须以原课件逐节点/逐箭头复核，联系表缩略图不足以判定一致。
- 第 59-65 页部分 IIR 结构图和系数标签在联系表中偏小；需结合高分辨率单页确认是否达到打印可读性，并与原课件比例进行比对。
- 第 73-96 页联系表发现明确分页风险：第 86 页仅保留 6.4.3 开头与少量文字，后约三分之二为空白，而同一节内容延续到第 87 页；这不是章尾允许留白，需检查第十一/十二批边界并重排。
- 第 88-89 页同属 6.5.1 理想滤波器，分别存在大面积页尾空白，内容具备重排合并空间；需在不压缩公式和图形的前提下重新分配两页内容。
- 第 82-83 页用户指定的两张 FIR 重要表均已完整独立保留，宏观未见截断；第 87 页零点图和第 90-92 页窗函数/吉布斯图仍需高分辨率检查标签与坐标轴间距。
- 第 97-120 页联系表未见明显越界或元素互压；第 101-102 页为本章思维导图和课后题，接近章尾，留白可接受。
- 第 97-99 页频率采样法连续三页内容较疏，第 109、111-113 页多采样率推导也有较大页尾空白；这些属于同章内部，需在保持公式字号和例题书写空间的前提下评估是否可合并或前移内容。
- 第 116-118 页 TDM/FDM 与定义页宏观拓扑和标签均在页面范围内，未见前次出现的文字不居中或输出裁切；仍需逐图对照原课件 428-430 页确认连线位置。
