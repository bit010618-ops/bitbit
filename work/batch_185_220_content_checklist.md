# Source PPT 185-220 Content Checklist


- 185: 第三章离散傅里叶变换封面，作为章节扉页/章节起始信号保留；去除水印和装饰网络背景。
- 186: 目录页，六个模块必须保留：离散傅里叶级数 DFS、离散傅里叶变换 DFT、各种变换的关系、频率采样、用 DFT 进行线性卷积、用 DFT 进行频谱分析。

- 187: 3.4 频率采样 / 3.4.1 频率采样定理。保留 z 变换定义 X(z)=sum x(n)z^{-n}，ROC 包含单位圆说明，以 2π/N 为间隔在单位圆上等间隔抽样得到周期序列 X~(k)；保留红色公式 (1)(2)(3) 与恢复问题说明。
- 188: 频率采样定理推导续。保留式 (1)(2)(3)，将 (2) 代入 (3)，红色式 (4)，以及由 1/N sum W_N^{k(m-n)} 得到周期延拓结论：x~_N(n)=sum_r x(n+rN)=...+x(n-N)+x(n)+x(n+N)+...；红色内容必须保留。

- 189: 频率采样结论页。根据 DFT/DFS 关系取主值序列，得到 X(k)=DFT[x~_N(n)R_N(n)]=DFT[x_N(n)]=X~(k)R_N(k)，以及 x_N(n)=IDFT[X~(k)R_N(k)]=IDFT[X(k)]=x~_N(n)R_N(n)。文字解释频域抽样导致时域周期延拓。结论黑/红两行必须突出：时域采样产生频域周期延拓；频域采样产生时域周期延拓。
- 190: 3.4 频率采样定理思考页。保留蓝色问题：对长度 N 有限序列 X(e^jw) 以 2π/N 等间隔采样形成周期序列 X~(k) 后，恢复情况如何；对无限长 x(n) 或以 2π/N0 且 N0<N 采样如何。保留结论：(1) x(n) 无限长序列一定混叠失真；(2) 长度 M 有限长序列：N>=M 不失真，N<M 失真。红色关键词保留。

- 191: 频率采样定理的有限长不失真条件。保留红色 N≥M 和恢复公式：x~_N(n)R_N(n)=IDFS[X~(k)]R_N(k)=x(n)，并说明频域采样可不失真恢复原信号，否则产生时域混叠。
- 192: 例题：设 x(n)=R_6(n)，对 X(z) 在单位圆上等间隔 4 点取样得到 X(k)，求 X(k) 的 IDFT x_4(n)。保留解法说明：对原序列做 4 点周期延拓并取主值序列；需要画/排出原 PPT 的周期延拓叠加列表，结果 x_1(n)={2,2,1,1}。

- 193: 3.4.2 频率内插。由 X(k) 可表示 x(n)，进而表示 X(e^jw) 和 X(z)。保留序列 x(n)=1/N sum X(k)W_N^{-kn}，X(z) 的三步推导，最终 X(z)=1/N sum X(k)(1-z^{-N})/(1-W_N^{-k}z^{-1})。保留结论：在数字滤波器结构和设计中，该表达式提供频域采样结构。
- 194: 3.5 用 DFT 计算线性卷积 / 3.5.1 循环卷积定理。保留 L 点循环卷积定义：y_c(n)=[sum_{m=0}^{L-1} x_2(m)x_1((n-m))_L]R_L(n)，L>=max[N1,N2]；保留用 L 表示、记号 y_c(n)=x_1(n) L-circle-conv x_2(n)。保留红色注意：学完线性卷积与循环卷积关系后，计算循环卷积先算线性卷积再利用关系。保留 DFT 的时域循环卷积定理：x(n)=x_1(n) N-circle-conv x_2(n)，X(k)=DFT[x(n)]_N=X_1(k)X_2(k)，不足 N 补零。

- 195: 用 DFT 求循环卷积说明。保留流程图：x(n) N1点、h(n) N2点分别补零到 L 点，做 L 点 DFT 得 X(k), H(k)，频域相乘得 Y(k)，再 L 点 IDFT 得 y(n)。保留红色重点：求系统输出都是用线性卷积；DFT 本身只能用来计算循环卷积；需要导出循环卷积和线性卷积相等条件，才能用 DFT 计算线性卷积。
- 196: 3.5.2 线性卷积与循环卷积的关系（星标）。保留假设 h(n), x(n) 有限长，长度 N/M。线性卷积公式 y_1(n)=h(n)*x(n)=sum h(m)x(n-m)。循环卷积公式 y_c(n)=h(n) L-circle-conv x(n)=[sum h(m)x((n-m))_L]R_L(n)。保留 L>=max[N1,N2]、周期延拓 x((n))_L=sum_i x(n+iL)、推导 yc(n)=[sum_i y1(n+iL)]R_L(n) 的红星重点。

- 197: 线性卷积与循环卷积关系续。保留红星公式 yc(n)=[sum_i y1(n+iL)]R_L(n)。红色核心结论：x(n) 与 h(n) 的 L 点循环卷积 yc(n) 等于其线性卷积 y1(n) 以 L 为周期延拓后取主值序列。说明 y1(n) 长度 N+M-1，只有 L>=N+M-1 时无时域混叠，yc(n)=y1(n)。保留条件 L>=N+M-1，以及红色关键词重叠相加法、重叠保留法。
- 198: 线性卷积/循环卷积关系示意图。应重画为清晰流程图而非照搬模糊截图：时域 h(n), x(n) -> y1(n)=x(n)*h(n)；频域 N点DFT 得 X(k), H(k)，相乘 Y(k)=X(k)H(k)，N点IDFT 得 y(n)；强调频域离散导致时域周期，定义圆周卷积 y(n)=x(n) N-circle-conv h(n)，以及 y(n) 与 y1(n) 的关系。

- 199: 例题：求 x1(n), x2(n) 的线性卷积和 5点、6点、7点循环卷积；x1(n)=R5(n), x2(n)=n+1(0<=n<=2)。保留解：y1(n)=x1*x2={1,3,6,6,6,5,3}；5点循环卷积={6,6,6,6,6}；6点循环卷积={4,3,6,6,6,5}；因为 7>=N1+N2-1，7点循环卷积等于线性卷积 {1,3,6,6,6,5,3}。保留红色注：求循环卷积前一般先求线性卷积 y1(n)。
- 200: 例题：50点长 x(n), 20点长 h(n) 分别做 50点 DFT，Y(k)=X(k)H(k)，y(n) 为 Y(k) 50点 IDFT。求 y(n) 与线性卷积一致的 n 范围。保留图示逻辑：线性卷积支撑 [0,68]，50点循环导致时域周期混叠；无混叠区间 n∈[19,49]，有混叠区间约 0-18。图需重画清楚，包括 x(n)*h(n): n∈[0,68]、50点循环结果 n∈[0,49]、有/无混叠标注。

- 201: 例题：有限长 x1(n) 在 2<=n<=5 区间外为零，x2(n) 在 1<=n<=4 区间外为零，求使 N 点循环卷积等于线性卷积的最小 N。保留解中区间列式：x1: n∈[2,5]，x2: n∈[1,4]，线性卷积 x1*x2: n∈[3,9]，循环卷积主值 n∈[0,N-1]；需推得最小 N 覆盖线性卷积长度/区间。
- 202: 3.5.3 重叠相加法（星标）。保留思路：x(n) 很长时，将 x(n) 分为每段长度 M 的序列，M 接近 h(n) 长度即可。保留公式 h(n)*sum_i x_i(n-iM)=sum_i h(n)*x_i(n-iM)=sum_i [h(n)*x_i(n)]*delta(n-iM)。保留结论：只要求出每段的线性卷积再移位相加即可得到 x(n) 与 h(n) 的线性卷积结果。
- 203: 重叠相加法图示。需重画清晰示意：1 原长序列 x(n) 与 h(n)；2 将 x(n) 分为 x1,x2,x3... 每段长度 M；3 每段与 h(n) 线性卷积得到 y1,y2,y3，每段长度 L=M+N-1；4 将 y_i 按 M 间隔移位后重叠相加得到 y(n)。保留标注 0, M-1, M, 2M-1, L-1, M+L-1, 2M+L-1 和“重叠相加”。
- 204: 用 DFT 实现重叠相加法步骤。保留三步：1 长序列分段，选 L>=M+N-1，对 xi(n), h(n) 补零到 L 点；2 计算 L 点 DFT，相乘，再 IDFT 得线性卷积；3 将每段线性卷积移位重叠相加。保留红色注意：使用 FFT 时，循环卷积点数 L 必须为 2 的 m 次幂，其余与 DFT 相同。
- 205: 重叠相加法例题。已知 x(n)=(n+1)R8(n), h(n)=R3(n)，求线性卷积并用重叠相加验证。保留左侧线性卷积列表，结果 {1,3,6,9,12,15,18,21,15,8}；保留右侧分段 x1/h、x2/h 的局部卷积列表 y1={1,3,6,9,7,4}, y2={5,11,18,21,15,8}，n=4 处重叠相加块，最终 {1,3,6,9,12,15,18,21,15,8}。红色流程提示保留：流程上用 IDFT 求线性卷积，但该题直接算线性卷积更方便验证。
- 206: 重叠相加法计算次数例题。3000 点序列与单位采样响应 60 点滤波器作线性卷积，采用 128 点 DFT/IDFT。保留计算：每段点数 M=128-60+1=69；3000/69≈43.48，取 44 段；每次需要算 xi(k) 和 h(k)，h(k) 只算一次；DFT 次数 44+1=45 次；每段 xi(k)h(k) 需一次 IDFT，共 44 次 IDFT。
- 207: 3.5.4 重叠保留法（星标）图示。需重画清晰图：1 x(n), h(n)，第一段前补/空出 N-1 点；2 分段 x1,x2,x3，每段包含前一段 N-1 个重叠点和 M 个新点，L0=M+N-1；3 每段循环卷积后丢弃前 N-1 点，只保留后 M 个点，红色标注“前 N-1 点存在混叠全部舍去”“重叠保留，只保留后 M 个点”；4 拼接 y1,y2,y3 得 y(n)。
- 208: 使用 DFT 实现重叠保留法的步骤。保留三步：1 对 x(n) 分段，选择每段长度 M，h(n) 长度 N，第一段空出 N-1 个点，后续每段与前一段有 N-1 个重叠点，对 h(n) 补零；2 每段 xi(n) 与 h(n) 做循环卷积，即用 DFT 求 L 点 xi(k), h(k)，相乘后 IDFT 得循环卷积，抛弃前 N-1 点，L>=L0 时多余点也舍弃；3 将截取后每段 M 点拼接得到线性卷积 y(n)。

### 原 PPT 209
- 3.5.4 重叠保留法例题：已知有限长序列 x(n), 0<=n<=7 与 h(n), 0<=n<=19，做 20 点循环卷积 yc(n)，线性卷积为 yL(n)，问 yc(n) 中哪些点与 yL(n) 相同。
- 需保留结论：线性卷积范围 [0,26]；20 点循环卷积是线性卷积以 20 为周期延拓后的主值序列；在 [0,6] 出现混叠，在 [7,19] 线性卷积和循环卷积相同。
- 重排时公式区间与变量下标要渲染为公式，红星保留为重点标记或“考点”提示，不保留水印。

### 原 PPT 210
- 3.5.4 重叠保留法例题：h(n) 长度 M=31，x(n) 长度 N1=19000；用重叠保留法计算线性卷积，将 x(n) 分段，每段 512 点，问 x(n) 有几段。
- 需保留两种思路：法一按线性卷积总输出点数算；法二从 x(n) 自身分段出发，其中法二只作思路提示，掌握法一即可。
- 关键计算：线性卷积点数 19000+31-1=19030；每段可求出点数 512-30=482；段数 19030/482 ≈ 39.48，因此段数为 40 段。所有除法使用分数形式渲染。

### 原 PPT 211
- 3.5.4 重叠保留法例题：已知 x(n)=(n+1)R_8(n), h(n)=R_3(n)，求两序列线性卷积，并用重叠保留法验证，其中 x(n) 分为若干段，每段长度为 6。
- 需保留左侧线性卷积竖式/结果：y(n)={1,3,6,9,12,15,18,21,15,8}。
- 需保留右侧重叠保留法示意：M=4, N=3, L0=M+N-1，分段/舍弃前 N-1 点/拼接的过程，以及底部结果 y(n)={1,3,6,9|12,15,18,21|15,8}。
- 红字重点保留：流程上用 IDFT 求循环卷积卷积，但该题直接算循环卷积更方便验证。

### 原 PPT 212
- 3.5.4 重叠保留法例题：已知 x(n)=13-n, 0<=n<=12；h(n)={2,-1,1}；将 x(n) 每段分为 7 点，用重叠保留法求系统输出。
- 需保留说明：h(n) 长度为 3，对 x(n) 分段，每段重复 2 点。
- 需保留分段序列：x0(n)={0,0,13,12,11,10,9}；x1(n)={10,9,8,7,6,5,4}；x2(n)={5,4,3,2,1,0,0}。
- 需保留局部卷积/循环卷积结果：y0(n)={1,9,26,11,23,21,19}；y1(n)={21,12,17,15,13,11,9}；y2(n)={10,3,7,5,3,1,1}。
- 需保留最终输出：y(n)={26,11,23,21,19,17,15,13,11,9,7,5,3,1,1}，并保留红字提示“流程上用 IDFT 求线性卷积，但该题直接算线性卷积更方便”。

### 原 PPT 213
- 3.5.4 重叠保留法例题：用长度 N=50 的 FIR 滤波器 h(n) 过滤长数据序列；要求采用重叠保留法通过 DFT/FFT 实现，对输入分段，每段 100 点；相邻两段重叠 V 点；每段与 h(n) 做 128 点循环卷积得到 ym(n)，最后从 ym(n) 选取 B 个样值连接得到输出 y(n)。
- 需保留问题：(1) 求 V；(2) 求 B；(3) B 个采样点应为 ym(n) 的哪些点。
- 需保留答案：V=49；B=51；采样点为 49~99。变量和下标用公式渲染。

### 原 PPT 214
- 3.5.5 求解 LTI 系统输出方法小结。
- 需保留三种方法：一、时域直接求解；二、频域解析法；三、DFT 求解法。
- 时域直接求解公式：y1(n)=h(n)*x(n)=sum_{m=0}^{N-1}h(m)x(n-m)。注意求和上限和变量清晰渲染。
- 频域解析法公式：y1(n)=IDFT[Y(e^{jw})]=IDFT[X(e^{jw})H(e^{jw})]（源 PPT 此处写 IDFT，重排时忠于源内容，可在校对记录标记是否应为 IDTFT/反变换待人工复核）。
- DFT 求解法需保留流程图：x(n) N1 点、h(n) N2 点分别补零到 L 点，做 L 点 DFT 得 X(k), H(k)，相乘得 Y(k)，再 L 点 IDFT 得 y(n)。重排时重画清晰框图，不能用模糊截图。

### 原 PPT 215
- 3.6 用 DFT 进行频谱分析。需保留说明：连续信号和系统的傅里叶分析无法直接用计算机计算，必须先抽样形成序列；序列过长需要截断；进行 DFT 分析时只能看到有限个值。
- 需保留三个失真过程及对应关系图：时域抽样 -> 频域混叠；时域截断 -> 频谱泄露（截断效应）；频域抽样（DFT）-> 栅栏效应。重排时可做成三行箭头示意，红色箭头保留。

### 原 PPT 216
- 3.6.1 频谱混叠。需保留红色小标题“产生原因”。
- 需保留原因：模拟信号采样不满足奈奎斯特定理，即 fs < 2fh；频域发生混叠；时域离散序列频谱在 omega=pi 附近发生频谱混叠；离散序列频谱无法分析出原本频谱信息，这种失真叫频谱混叠失真。
- 需保留解决方法：1. 更改 fs，使 fs >= 2fh；2. fs 固定时，采样前预滤波，滤除大于 fs/2 的频率分量。所有 fs、fh、omega、pi、fs/2 用公式形式渲染。

### 原 PPT 217
- 3.6.1 频谱混叠下补充“频率分辨率”：指将相距最近的两个频率分量区分开的能力。从 DFT 看，DFT 处理相当于频谱等间隔取样，相邻两点的模拟频率差用 F 表示。
- 例题题干分两问：对实信号进行谱分析，要求谱分辨率 F<=10Hz，信号最高频率 fc=2.5kHz，确定最小记录时间 Tp_min、最大采样间隔 Tmax、最小采样点数 Nmin；第二问 fc 不变，谱分辨率提高一倍，求最小采样点数和最小记录时间。

### 原 PPT 218
- 频谱分辨率例题解答。
- 第一问保留计算：fs/N <= 10；fs >= 2fc = 5kHz；Tp=N/fs >= 0.1，因此 Tp_min=0.1；Tmax=1/(2fc)=1/5000；N >= fs/10，Nmin=500。
- 第二问保留说明：分辨率提高一倍即要使分辨的能力变强，F<=5Hz；fs/N<=5；N/fs>=0.2；Tp_min=0.2；Nmin=0.2*5000=1000。
- 红字重点保留：注意：若使用 FFT，Nmin=1024。所有频率、时间、采样点公式渲染，除法用分数。

### 原 PPT 219
- 3.6.2 截断效应。需保留红色小标题“产生原因”。
- 需保留说明：实际遇到的离散时间序列往往非时限，处理时需要截断；截断就是将序列 x(n) 乘上窗函数 w(n)，形成 y(n)=x(n)w(n)。
- 需保留例子：w(n)=R_N(n), x(n)=cos(w0 n)。频谱变化公式包括 W(e^{jw})、|W(e^{jw})| 和 X(e^{jw}) 的冲激表达；公式需清晰渲染，不能过小。
- 需保留下方谱图：在 -w0 和 w0 处两根冲激，标注 pi、X(e^{jw})、w；重排时可重画为黑色坐标轴和冲激 stem 图。

### 原 PPT 220
- 3.6.2 截断效应。需保留红色文字“截断后，会产生两方面影响”。
- 第一方面：产生频谱泄露。窗谱主瓣使原谱线展宽；截断长度越长，展宽越窄，泄露越小；泄露会使频率分辨率降低。
- 第二方面：截断后产生谱间干扰。矩形窗截断时旁瓣不小，旁瓣可淹没弱信号主瓣。
- 需保留红色小标题“应对截断效应的措施”：1. 减小主瓣宽度/减小泄露，采用更长截断长度，即加大窗宽；2. 减小旁瓣幅度，使用缓变型窗函数截断，例如海明窗。

### 原 PPT 221
- 3.6.3 栅栏效应。需保留说明：N 点 DFT 是 DTFT 在 [0,2pi] 的 N 点等间隔采样，采样点之间频谱不可见，如同从 N 个栅栏缝隙观察频谱，只能得到 N 个缝隙中看到的函数值，这种现象称为栅栏效应。
- 需保留红色小标题“应对栅栏效应的措施”：1. 在序列尾部补零，可看到更多谱线，但不能提高频率分辨率；频率分辨率要从增加截取长度入手。2. 采样频率不变，增加采样长度，即 DFT 点数增加。

### 原 PPT 222
- 3.6 频谱分析真题提示页。需保留年份和问题：
  2007 真题：频谱泄露产生的主要原因是什么？可以用什么方法改善？
  2016 真题：栅栏效应是什么？简述减小方法。
  2019 真题：简述栅栏效应的原因和解决方法。
  2020 真题：什么是栅栏效应？
  2021 真题：什么是频谱泄露，怎么抑制？
- 重排时可作为“814 考点提示/历年真题关注点”放在本节末尾，不能删除。

### 原 PPT 223
- 3.6 频谱分析综合例题：已知连续信号 x(t) 的频率成分集中在 0-2000Hz，利用 DFT 谱分析，要求指出参数如何选取；本页保留第 (3) 问：简述利用 DFT 分析连续时间信号 x(t) 的频谱时会引起哪些误差，如何改善。
- 需保留答案：会引起 3 种误差：频谱混叠（提高抽样率或进行抗混叠滤波）、截断效应（选择合适的窗函数以及增加截断长度）、栅栏现象（补零）。

### 原 PPT 224
- 第三章/频谱分析小结图。需保留完整关系图：xa(t) 经过抽样 t=nT 得 x(n)，截断得 x(n)w(n)，周期延拓得 \tilde{x}_N(n)，取一个周期得 x_N(n)；下方频域 Xa(jΩ)、X(e^{jω})、X(e^{jω})*W(e^{jω})、\tilde{X}_N(k)、X_N(k) 之间的 FT/DTFT/DFS/DFT 对应关系。
- 需保留箭头说明：时间抽样对应频域周期延拓，时域截断对应频域卷积，频域抽样对应栅栏效应；图中还包括 omega=2pi/N*k、Ω=2pi/T 等公式标注。
- 重排时重画为清晰流程图/上下双域图，去除水印但不删关系。

### 原 PPT 225
- 小结思考题：已知 x1(n)={1,1,1}, x2(n)={1,1,1,0,0,0,1}。
- 需保留问题：(1) 计算 x1(n) 和 x2(n) 的线性卷积和 7 点圆周卷积；(2) 什么条件下线性卷积等于圆周卷积。
- 需保留答案：(1) {2,3,3,2,1,0,1}；(2) N>=9。题干序列和答案用公式/等宽清晰排版，不删。

### 原 PPT 226
- 第三章章节导图。需保留主题“离散傅里叶变换”，分为上/下两部分。
- 左侧需保留：DFS 提醒（计算机只能处理离散数据，引出 DFS；DTFT 时域离散、对频率采样得到 DFS）、DFS 定义/性质；DFT 定义/性质（线性、循环移位、时域与频域性质、圆周共轭对称性质、常用变换对）、提醒（DFT 是 DFS 取主值区间得到的主值序列；周期序列只需知道一个周期的信息，就能得到整个周期序列）、各变换关系。
- 右侧需保留：频率采样（频率采样定理、频域内插）、循环卷积（定义/定理/求解思路，与线性卷积关系和二者相等条件）、DFT 求解系统输出（利用 DFT 求解线性卷积、长序列 DFT 卷积：重叠相加法/重叠保留法）、频谱分析（DFT 对模拟信号频谱分析、问题与措施：频谱混叠、截断效应、频谱泄露、谱间干扰、栅栏效应）。星标重点保留。
- 重排时可改为 A4 纵向导图/分栏总结，但不能删节点。

### 原 PPT 227
- 第三章课后习题清单，必须放在第三章末尾，不放在下一章章前。
- 需保留题目列表：3-3 画序列图像并求循环卷积；3-13 Z 变换单位圆上等间隔采样求 IDFT；3-14 循环卷积和线性卷积的关系；3-17 频率采样；3-18 谱分析；3-19 谱分析；3-21 使用 FFT 的重叠保留法；3-22 证明频域循环卷积定理；3-24 时域卷积定理；3-25 循环卷积和线性卷积的关系。

### 原 PPT 228
- 第四章封面：快速傅里叶变换。作为下一章起点，不并入第三章末尾习题之前；生成完整讲义时可作为第四章章首页。
