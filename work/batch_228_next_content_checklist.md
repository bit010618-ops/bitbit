# 第八批源内容核对：原 PPT 228 起（第四章 快速傅里叶变换）

目标：从第四章封面开始，继续按“不删减源内容，只做版式重排和明确错误修正”的原则核对源 PPT。公式、流程图、红字重点、例题步骤和课后题均需完整保留。

### 原 PPT 228
- 第四章封面：快速傅里叶变换。重排时作为第四章章首页，去除水印和装饰背景，保留章节名。

### 原 PPT 229
- 第四章目录：1 快速傅里叶变换的定义；2 快速傅里叶变换的分类；3 快速傅里叶反变换；4 实序列的快速傅里叶变换。
- 重排时可作为第四章开篇目录，不保留装饰背景和水印。

### 原 PPT 230
- 4.1 快速傅里叶变换的定义。思考题：设有限长序列 x(n)，非零值长度为 N，若对 x(n) 进行一次 DFT 运算，共需多大的运算工作量？那么 N 点呢？
- 需保留“思考”提示含义，并将 x(n)、N、DFT 等按正文/公式混排清楚。

### 原 PPT 231
- 快速傅里叶变换定义继续：由上节知识可知，长度为 N 的有限序列 DFT 表达式为 X(k)=sum x(n)W_N^(kn)，0<=k<=N-1。
- 源页将 k=0、k=1、k=2、...、k=N-1 的展开式逐行列出，右侧括注“一共 N 个点”，底部括注每个点需要 N 次复乘、N-1 次复加。
- 重排时必须保留逐行展开和两个大括注的含义，可改为 A4 讲义式矩阵/分组公式，去水印，不要压缩到看不清。

### 原 PPT 232
- 快速傅里叶变换定义继续：提出“设有限长序列 x(n)，非零值长度为 N，若对 x(n) 进行一次 DFT 运算，共需多大的运算工作量？那么 N 点呢？”
- 保留表格：一 点 DFT 复数乘法 N、复数加法 N-1；N 点 DFT 复数乘法 N^2、复数加法 N(N-1)。
- 保留结论说明：当 N=1000 点，需要复乘 1000000 次，处理点数多、计算量和内存要求大，难以实时处理，限制 DFT 应用，因此提出 FFT 用于减少计算量。

### 原 PPT 233
- FFT 用途：用于减少计算量，但本质还是 DFT。FFT 仅仅是 DFT 的一种快速算法。该句源页为红字重点，必须保留强调。
- FFT 提高运算效率的本质：利用 W_N^(kn) 的周期性和对称性，将长序列分解为短序列。源页中“周期性和对称性”为红字重点，重排时保留重点色或等价强调。

### 原 PPT 234
- W_N^(kn) 的相关性质，标题为红字重点。
- 保留定义式：W_N^(kn)=e^(-j 2pi/N kn)。
- 对称性：W_N^(-kn)=W_N^((N-n)k)=W_N^((N-k)n)。
- 周期性：W_N^(kn)=W_N^((N+n)k)=W_N^((N+k)n)。
- 可约性：W_N^(kn)=W_(Nm)^(knm)，以及 W_N^(kn)=W_(N/m)^(kn/m) 这一组性质。重排时把分式指数渲染清楚。
- 特殊点：W_N^0=W_N^N=1，W_N^(N/2)=-1，W_N^(k+N/2)=-W_N^k。

### 原 PPT 235
- 长序列分解为短序列来减少计算量的示意图。
- 保留流程含义：N 点 DFT 分成两个 N/2 点 DFT，再分成四个 N/4 点 DFT，后续继续分解。
- 保留复乘次数对比：原 N^2；分成两个 N/2 点后为 (N/2)^2+(N/2)^2=N^2/2；分成四个 N/4 点后为四个 (N/4)^2 合计 N^2/4；后续继续下降。
- 重排时可重画为简洁流程图，但必须保留红色箭头/运算量递减关系和每一级 DFT 方块，不能只给结论。

### 原 PPT 236
- 4.2 快速傅里叶变换的分类。
- 保留分类：FFT 分为时间抽取法 DIT（Decimation-In-Time）和频率抽取法 DIF（Decimation-In-Frequency）。
- 重排时可用讲义式分支图，保留中文名称、英文缩写和英文全称。

### 原 PPT 237
- 基于时间抽取的基-2-FFT 快速算法。
- 算法思路：设输入序列长度 N=2^M（M 为正整数），将该序列按时间顺序的奇偶分解为越来越短的子序列，称为基 2 按时间抽取的 FFT 算法。
- 源页红字重点包括“奇偶分解”“基2按时间抽取的FFT算法”“加零补长”，重排时必须保留强调。
- 注意：其中基 2 表示 N=2^M，M 为整数。若不满足这个条件，可以人为加若干零值（加零补长）使其达到 N=2^M。

### 原 PPT 238
- 基于时间抽取的基-2-FFT 快速算法的设计原理：设 N=8，则 M=3。
- 保留原流程图含义：原序列 x(0) 至 x(7) 按偶序号 x(2r) 和奇序号 x(2r+1) 分成 x1(r)、x2(r) 两组。
- 偶支：x(0)=x1(0), x(2)=x1(1), x(4)=x1(2), x(6)=x1(3)，进入 4 点 DFT，得到 X1(0) 至 X1(3)。
- 奇支：x(1)=x2(0), x(3)=x2(1), x(5)=x2(2), x(7)=x2(3)，进入 4 点 DFT，得到 X2(0) 至 X2(3)。
- 保留右侧 X(k) 输出列 X(0) 至 X(7) 和中间问号合并箭头；后续页应解释如何由 X1、X2 合成 X。

### 原 PPT 239
- 基于时间抽取的基-2-FFT 设计原理推导。
- 保留起始式：X(k)=DFT[x(n)]_N=sum_{n=0}^{N-1} x(n) W_N^(kn)。
- 按奇偶一次分解：偶数部分 x(2r) 为 N/2 点，记为 x1(r)；奇数部分 x(2r+1) 为 N/2 点，记为 x2(r)。
- 保留完整推导链：X(k)=sum x(2r)W_N^(k2r)+sum x(2r+1)W_N^(k(2r+1)) = sum x1(r)W_N^(k2r)+W_N^k sum x2(r)W_N^(k2r) = sum x1(r)W_(N/2)^(kr)+W_N^k sum x2(r)W_(N/2)^(kr) = X1(k)+W_N^k X2(k)。
- 红字注意必须保留：这里 k 的范围是 0<=k<=N/2-1，仅仅是 N 的前一半。

### 原 PPT 240
- 后半频点推导：令 k=k+N/2，则 k+N/2 的范围为 N/2<=k+N/2<=N-1，是 N 的后一半。
- 保留公式：X(k+N/2)=X1(k+N/2)+W_N^(k+N/2) X2(k+N/2)。
- 利用 DFT 周期性：X1(k+N/2)=X1(k)，X2(k+N/2)=X2(k)。
- 保留结论：X(k+N/2)=X1(k)-W_N^k X2(k)，适用于 N/2<=k+N/2<=N-1。
- 重排时公式字号要足够大，上下标和 N/2 范围不能压小。

### 原 PPT 241
- 基于时间抽取的基-2-FFT 设计原理总结。
- 红字保留：前半部分 X(k)=X1(k)+W_N^k X2(k)，后半部分 X(k+N/2)=X1(k)-W_N^k X2(k)，0<=k<=N/2-1。
- 引入蝶形运算符号（Butterfly Computation）：保留蝶形图，输入 X1(k)、X2(k)，旋转因子 W_N^k，输出 X1(k)+W_N^k X2(k) 和 X1(k)-W_N^k X2(k)。
- 保留表格：一个蝶形运算需要复数乘法 1 次、复数加法 2 次。

### 原 PPT 242
- N=8 DIT-FFT 一次抽取后的完整合成示意图。
- 保留左侧原序列分成 x1(r)、x2(r)，两个 4 点 DFT 得到 X1(k)、X2(k) 后，通过 N/2 个蝶形合成为 X(0) 至 X(7) 的网络。
- 保留旋转因子标注 W_8^0、W_8^1、W_8^2、W_8^3 和“N/2 个蝶形”标注。
- 保留底部运算量对比：直接计算 N^2=64 次复数乘法；一次抽取 (N/2)^2+(N/2)^2+N/2=4^2+4^2+4=36 次复数乘法。
- 保留“运算量明显减少！”提示含义。重排时可去除装饰气泡，但不能删除结论。

### 原 PPT 243
- 继续分解：将图中的 x1(r) 和 x2(r) 再次进行奇偶抽取，化解成两点 DFT。
- 保留定义：x3(l)=x1(2l)，x4(l)=x1(2l+1)，x5(l)=x2(2l)，x6(l)=x2(2l+1)，各自 l=0,1,2,3,...,N/4-1。
- 保留右侧说明：x1(r) 分解为奇偶两部分；x2(r) 分解为奇偶两部分。
- 对 x1(r) 分解为奇偶两部分做 DFT 变换的推导必须保留，最终得到 X(k)=X3(k)+W_(N/2)^k X4(k)。
- 红字注意保留：这里 k 的范围是 0<=k<=N/4-1。

### 原 PPT 244
- 继续 x1(r) 的后半频点推导：令 k=k+N/4，范围为 N/4<=k+N/4<=N/2-1。
- 保留公式：X(k+N/4)=X3(k+N/4)+W_(N/2)^(k+N/4)X4(k+N/4)=X3(k)-W_(N/2)^k X4(k)。
- 红字总结保留：前半部分 X(k)=X3(k)+W_(N/2)^k X4(k)，后半部分 X(k+N/4)=X3(k)-W_(N/2)^k X4(k)，0<=k<=N/4-1。
- 同理保留 x2(r) 分解为奇偶两部分做 DFT 的结果：前半部分 X(k)=X5(k)+W_(N/2)^k X6(k)，后半部分 X(k+N/4)=X5(k)-W_(N/2)^k X6(k)，0<=k<=N/4-1。
- 生成时注意这里源页公式密集，必须拆分成可读段落，不压缩上下标。

### 原 PPT 245
- 基 2-DIT-FFT 算法蝶形流图，N=8，M=3。
- 保留输入排列顺序：x(0), x(4), x(2), x(6), x(1), x(5), x(3), x(7)，右侧输出 X(0) 至 X(7)。
- 保留三列蝶形级，每级标注 N/2 个蝶形，底部括注 M 级蝶形运算，右上角 M=log_2 N。
- 保留旋转因子标注 W_8^0、W_8^1、W_8^2、W_8^3 等。
- 保留红字复数乘法次数公式：基 2-DIT-FFT 算法复数乘法次数为 (N/2)*M=(N/2)log_2 N。

### 原 PPT 246
- 直接计算 DFT 和 DIT-FFT 的计算量比较。
- 保留表格：DFT 的复数乘法为 N^2，复数加法为 N(N-1)；FFT 的复数乘法为 (N/2)log_2 N，复数加法为 N log_2 N。
- 表格中公式为红色重点，重排时公式须渲染为可读数学形式，不要压小上下标。

### 原 PPT 247
- 计算量比较例题：通用计算机平均每次复乘需要 1 us，每次复加需要 0.1 us，用它计算 64 点 DFT[x(n)]，问直接用 DFT 和用 FFT 分别需要多少时间。
- 保留直接 DFT 计算：复乘 T1=N^2*1*10^-6=4.096*10^-3 秒；复加 T2=N(N-1)*0.1*10^-6=4.032*10^-3 秒；T=T1+T2=8.128*10^-3 秒。
- 保留 FFT 计算：复乘 T1=(N/2)log_2 N*1*10^-6=1.92*10^-4 秒；复加 T2=N log_2 N*0.1*10^-6=3.84*10^-4 秒；T=T1+T2=5.76*10^-4 秒。
- 重排时题干中的 1 us、0.1 us 和 64 点 DFT 均需清楚呈现，公式用数学渲染。

### 原 PPT 248
- 基于频域抽取的基-2-FFT 快速算法的设计原理。
- 设计思路：设序列 x(n) 长度为 N=2^M，先将 x(n) 前后对半分开，得到两个子序列，再做 DFT 计算。
- 保留推导：X(k)=sum_{n=0}^{N/2-1} x(n)W_N^(kn)+sum_{n=N/2}^{N-1}x(n)W_N^(kn)，化为 sum x(n)W_N^(kn)+sum x(n+N/2)W_N^(k(n+N/2))，再化为 sum [x(n)+x(n+N/2)(-1)^k] W_N^(kn)，0<=k<=N-1。
- 按 k 的奇偶分为两部分：k=2r 或 k=2r+1，r=0,1,2,...,N/2-1。
- 红字公式保留：k 为偶数时 X(2r)=sum_{n=0}^{N/2-1}[x(n)+x(n+N/2)]W_(N/2)^(rn)；k 为奇数时 X(2r+1)=sum_{n=0}^{N/2-1}[x(n)-x(n+N/2)]W_(N/2)^(rn)W_N^n。

### 原 PPT 249
- DIF 设计原理继续。
- 定义 x1(n)=x(n)+x(n+N/2)，x2(n)=[x(n)-x(n+N/2)]W_N^n，n=0,1,2,3,...,N/2-1。
- 保留结论：k 为偶数时 X(2r)=sum x1(n)W_(N/2)^(rn)=X1(k)；k 为奇数时 X(2r+1)=sum x2(n)W_(N/2)^(rn)=X2(k)。
- 保留底部 DIF 蝶形图：输入 x(n)、x(n+N/2)，输出 x1(n)=x(n)+x(n+N/2) 与 x2(n)=[x(n)-x(n+N/2)]W_N^n，中间标注 W_N^n。

### 原 PPT 250
- DIF-FFT 一次分解运算流图，N=8。
- 保留 x(0) 至 x(7) 输入、第一次前后对半蝶形交叉网络、旋转因子 W_N^0 至 W_N^3，以及两个 4 点 DFT。
- 保留输出顺序：上支输出 X(0), X(2), X(4), X(6)，下支输出 X(1), X(3), X(5), X(7)。
- 重画时要区分 DIF 与 DIT：DIF 是输入自然顺序、输出按偶奇频点分组，不要画成 DIT 的倒位序输入。

### 原 PPT 251
- DIF-FFT 运算流图，N=8。
- 保留完整三阶段蝶形网络，输入 x(0) 至 x(7) 为自然顺序，输出为 X(0), X(4), X(2), X(6), X(1), X(5), X(3), X(7) 的分组顺序。
- 保留各级旋转因子标注 W_N^0、W_N^1、W_N^2、W_N^3 等。
- 保留结论：频率抽取法和时间抽取法总的计算量是相同的。

### 原 PPT 252
- 4.3 快速傅里叶反变换（IFFT）。
- 保留 DFT 正变换公式：X(k)=DFT[x(n)]_N=sum_{n=0}^{N-1}x(n)W_N^(kn)，0<=k<=N-1。
- 保留 IDFT 公式：x(n)=IDFT[X(k)]_N=(1/N)sum_{k=0}^{N-1}X(k)W_N^(-kn)，0<=n<=N-1。
- 保留三点差异：旋转因子 W_N^k -> W_N^(-k)；系数 1/N；X(k) -> x(n)。
- 重排时 `1/N` 必须为分数形式，不能用斜杠。

### 原 PPT 253
- DIF-IFFT 运算流图。
- 保留输入 X(0) 至 X(7)，输出 x(0), x(4), x(2), x(6), x(1), x(5), x(3), x(7)。
- 保留各级旋转因子 W_N^0、W_N^-1、W_N^-2、W_N^-3 等，以及每个输出支路的 1/N 系数。
- 重排时该图与 DIF-FFT 流图相似，但旋转因子指数为负且输出端乘 1/N，不能漏。

### 原 PPT 254
- 线性卷积和 FFT 方法求解 LSI 系统输出的运算量对比。
- 红字保留：线性卷积直接法运算量 N1*N2。
- FFT 方法流程图：x(n) N1 点补零至 N 点，N 点 FFT 得 X(k)；h(n) N2 点补零至 N 点，N 点 FFT 得 H(k)；频域相乘得到 Y(k)，再 N 点 IFFT。
- 保留条件 N>=N1+N2-1。
- 保留输出关系：y(n)=IDFT[X(k)H(k)]=x(n) 圆周卷积_N h(n)=y_l(n)。
- 保留 FFT 方法乘法次数：3*(N/2)log_2 N + N。

### 原 PPT 255
- 例题：长度 N=8000 的复序列 x(n) 与长度 N=100 的复序列 h(n) 卷积。
- 三问必须保留：（1）直接进行卷积所需复数乘法次数；（2）若采用基 2-FFT 算法求解系统输出，画出运算框图；（3）若用 1024 点基 2-DIT-FFT 重叠相加法计算卷积，试求需要多少次复数乘法。

### 原 PPT 256
- 例题解答。
- 第（1）问：复数乘法次数为 8000*100=8*10^5。
- 第（2）问：保留运算框图：x(n)、h(n) 均补零至 8192 点，分别做 8192 点 FFT 得 X(k)、H(k)，相乘得 Y(k)，再做 8192 点 IFFT 得 y(n)。
- 第（3）问：设 x(n) 每段长度为 M，h(n) 长度 N=100，则 M+N-1=1024，所以 M=925。
- 保留段数计算：ceil(8000/M)=9，故需要 10 次 FFT、9 次 IFFT。
- 保留总复数乘法次数：(10+9)*(1024/2)*log_2 1024 + 9*1024 = 106496。

### 原 PPT 257
- 4.4 实序列的 FFT 算法。
- 保留说明：实际工作中输入信号 x(n) 通常是实序列；若直接按 FFT 运算流图计算，把 x(n) 看作虚部为零的复序列，会增加存储量和运算时间。
- 保留两种处理方法：一次 N 点 FFT 计算两个 N 点实序列的 FFT；一次 N/2 点 FFT 计算一个 N 点实序列的 FFT。
- 源页红字重点为“实序列”“虚部为零的复序列”和两种方法标题，重排时保留强调。

### 原 PPT 258
- 一次 N 点 FFT 计算两个 N 点实序列的 FFT。
- 思路：一个实输入作为实部，另一个实输入作为虚部，计算完成后将结果简单分解得到各自 FFT 结果。
- 保留构造：y(n)=x1(n)+j x2(n)，Y(k)=Y_ep(k)+Y_op(k)。
- 保留时域分解：x1(n)=Re[y(n)]=(1/2)[y(n)+y^*(n)]；j x2(n)=j Im[y(n)]=(1/2)[y(n)-y^*(n)]。
- 由 DFT 共轭对称性质得到：X1(k)=Y_ep(k)=(1/2)[Y(k)+Y^*(N-k)]；X2(k)=(1/j)Y_op(k)=(1/(2j))[Y(k)-Y^*(N-k)]。
- 所有比值均使用分数形式，尤其 1/2、1/j、1/(2j)。

### 原 PPT 259
- 例题：设 x1(n) 和 x2(n) 都是 N 点实序列，试用一次 N 点 DFT 运算计算它们各自的 DFT。
- 保留构造：令 y(n)=x1(n)+j x2(n)，对 y(n) 进行 N 点 DFT 得到 Y(k)=Y_ep(k)+Y_op(k)。
- 保留共轭对称分解：Y_ep(k)=(1/2)[Y(k)+Y^*(N-k)]，Y_op(k)=(1/2)[Y(k)-Y^*(N-k)]。
- 保留最终结果：X1(k)=Y_ep(k)=(1/2)[Y(k)+Y^*(N-k)]；X2(k)=(1/j)Y_op(k)=(1/(2j))[Y(k)-Y^*(N-k)]。
- 源页“由 X(K) 可以求得”按上下文应为由 Y(k) 分解得到两个实序列的 N 点 DFT，生成时可按明确上下文修正。

### 原 PPT 260
- 一次 N/2 点 FFT 计算一个 N 点实序列的 FFT。
- 思路：对 N 点实序列分解，用一次 N/2 点 FFT 完成运算。取 x(n) 的偶数点和奇数点分别作为新构造序列 y(n) 的实部和虚部。
- 保留公式：x1(n)=x(2n)，x2(n)=x(2n+1)，y(n)=x1(n)+j x2(n)，0<=n<=N/2-1。
- 对 y(n) 进行 N/2 点 DFT：X1(k)=DFT[x1(n)]=Y_ep(k)，X2(k)=DFT[x2(n)]=(1/j)Y_op(k)，0<=k<=N/2-1。
- 红字注意保留：又 x(n) 为实序列，因此 X(k) 具有共轭对称性，X(N-k)=X^*(k)。

### 原 PPT 261
- 一次 N/2 点 FFT 计算一个 N 点实序列 FFT 的最终合成。
- 保留说明：这里奇偶抽取相当于基于时间抽取的基 2-FFT 快速算法，通过蝶形运算可以得到上半以及下半 X(k)。
- 保留蝶形图：输入 X1(k)、X2(k)，旋转因子 W_N^k，输出 X1(k)+W_N^k X2(k) 和 X1(k)-W_N^k X2(k)。
- 红字结论保留：前半部分 X(k)=X1(k)+W_N^k X2(k)；后半部分 X(k+N/2)=X1(k)-W_N^k X2(k)。

### 原 PPT 262
- 例题：简要推导采用 DIT 的基 2 FFT 将 8 点 DFT 分解为两个 4 点 DFT 计算的过程，并画出分解后的算法流图。
- 解：令 x1(n)=x(2n)，x2(n)=x(2n+1)，其中 n in [0,3]；且 y(n)=x1(n)+j x2(n)。
- 对 y(n) 进行 4 点 FFT，得到 Y(k)=Y_ep(k)+Y_op(k)。
- 保留分解关系：X1(k)=Y_ep(k)，X2(k)=-jY_op(k)，k in [0,3]。
- 保留合成公式：X(k)=X1(k)+W_8^k X2(k)，X(k+4)=X1(k)-W_8^k X2(k)，以及右侧蝶形图。
- 例题继续页需完整接上算法流图。

### 原 PPT 263
- 第四章章节导图。
- 中心为快速傅里叶变换 FFT，分支包括 FFT 定义、FFT 分类、快速傅里叶反变换 IFFT、实序列的 FFT 算法。
- FFT 定义分支标注“与 DFT 运算量比较”；FFT 分类分支包含基于时间抽取的基 2-FFT 快速算法、基于频率抽取的基 2-FFT 快速算法，并标注“会画图会计算”；实序列 FFT 算法分支包含一次 N 点 FFT 计算两个 N 点实序列的 FFT、一次 N/2 点 FFT 计算一个 N 点实序列的 FFT。
- 导图中星标处为重点，重排时保留重点含义。

### 原 PPT 264
- 第四章课后习题页。
- 书上课后题很少：4-3（一次 N 点 FFT 计算两个 N 点实序列的 FFT，且反着说），4-4 第（1）问（一次点 FFT 计算一个 N 点实序列的 FFT），第（2）问感兴趣的同学可以做下。
- 补充习题：习 4.1 FFT 是利用什么来减少计算量；习 4.2 分别画出 8 点基 2 DIT-FFT 和 DIF-FFT 的运算流图；习 4.3 直接计算 N=16 的 DFT 需要多少次复数乘法/复数加法，使用基 2 FFT 算法需要多少次复数乘法。
- 源页说明“习题答案在下一页”，因此下一页答案必须一并放在第四章末尾。

### 原 PPT 265
- 第四章课后习题答案页。
- 保留答案：解 4.1 为 W_N^(kn) 的周期性和对称性，并注明“不是蝶形运算！！”。
- 保留答案：解 4.2 略，运算流图可参考课件。
- 保留答案：解 4.3 为 256、240、32。
- 该页应作为第四章末尾内容，不能放到第五章前言之外或删除。

### 原 PPT 266
- 第五章封面：IIR 系统的网络结构与滤波器设计。
- 第八批到原 PPT 265 结束；原 PPT 266 起应作为下一批/下一章起点。
